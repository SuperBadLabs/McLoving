# Jenkins compatibility worker

This directory will contain the isolated JVM/Clojure Jenkins compatibility
compiler. It has no controller database, agent, scheduler, or execution-secret
authority. The current namespace is a foundation placeholder only.
