# Tool pins the dogfood lanes fetch themselves and Foundation gets from an
# action: the Clojure CLI is the setup-clojure `cli:` pin of foundation.yml,
# verified by scripts/dogfood/verify-lanes.py. tools/versions.env carries the
# pins shared with Foundation's own steps and is frozen by the retained
# observation receipt, so the dogfood-only pins live here.
CLOJURE_CLI_VERSION="1.12.4.1618"
CLOJURE_CLI_SHA256="13769da6d63a98deb2024378ae1a64e4ee211ac1035340dfca7a6944c41cde21"
