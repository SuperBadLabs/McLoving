# ADR 0015: Repository and roadmap

Status: Accepted

McLoving begins as a private greenfield monorepo. The first truthful vertical
slice is one strict-YAML Linux process flowing through canonical IR,
PostgreSQL, transactional outbox, scheduler, outbound agent lease, durable
journal, logs, result, restart reconciliation, and cancellation evidence.
