# ADR 0009: Security model

Status: Accepted

OIDC human identities, scoped service identities, centralized Rust
authorization, tenant isolation, trust-classed agent pools, attempt-scoped
secret grants, protected environments, and append-only audit form the security
model. Authentication never implies authorization.
