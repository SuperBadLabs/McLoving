# JCOMP-002C bounded correction evidence

These observations bind tested source `2326a63b6c52a5354a82d58aee9879db046613c9`, tree `035210fe048f45604119f796a18b47070006bff9`. Later evidence-only commits do not relabel this tested source. The ticket remains ACTIVE pending protected PR and exact-main gates. This compiler-only validation does not close JCOMP-003 or claim runtime equivalence.

The exact source was built into worker `8380567bcea6319ad204adfa1c319d715a0233f451cb7bf7ddce837f874949d0` and admission executable `d7e9c82e7a4c5c27d9410d1508ce240adf1e3670af2afa187a0439d37d63f915`. All 228 historical identities were captured and independently replayed using 226 exact original representations and two previously retained redacted representations: 1 admitted, 216 unsupported, 11 rejected, zero unverified. Only six previous unverified rows changed. Their source identities/hashes appear in the campaign and six-case receipt; repository source bytes are referenced, not duplicated here. Before replies are actual worker observations that genuine admission refused; they remain unverified historical observations.

All 23 preregistered fixtures were compiled twice and independently replayed: 10 authored positive fixtures plus historical C052, and 12 negative fixtures. Status/reason, executable pipeline YAML and semantic IR hashes match the historical campaign. Disabled definition YAML embeds the new V2 contract hash and therefore changes; genuine admission reconstructs those bytes independently. No workload was executed.

The included campaign manifests and source-qualified receipts summarize complete local validation. This deliberately bounded public directory omits the other original-source response bodies, the 93-file authored raw capture, full source archive, admission executable, image layers and build inspection payloads. They are retained locally under `/tmp/mcloving-jcomp002c-evidence` for the durable mission handoff. This directory alone is not a complete offline replay inventory. Final JCOMP-003 campaigns must refresh source/build/runtime evidence after this correction merges.

The external original-corpus helper differs from the historical reviewed tool only in its exact checkout root and V2 contract pin; both tool digests are retained in the exact-build receipt. It uses the same contained launcher, population/context binding and genuine admission checks. Historical V1 evidence remains bound to V1, whose contract bytes were preserved.

Intermediate failures are retained locally and described in `local-verification-2326.json`: an initial wrong-directory Clojure invocation, an updated test-population expectation after new tests, an intermediate editing syntax error fixed before commit, and an overstrict historical comparison that initially expected the contract-bound disabled-definition hash to remain unchanged. Final exact-source tests passed. Both intentionally mutated regression runs failed as expected and their failure logs are included. None of these failures was replaced or relabeled as a successful original run.

`source-2326.bundle` preserves the exact tested source independently of future squash history. It is incremental from protected base `44f0498fbdf3a59434176e9d09a52e1260336260`; obtain that base first, then follow `source-bundle-recovery.json`. A fresh isolated recovery reproduced the exact commit, tree and full source archive hash. The bundle adds source recoverability only, not the omitted raw campaign inventory or execution tools.
