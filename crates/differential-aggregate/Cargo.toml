[package]
name = "mcloving-differential-aggregate"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
mcloving-boundary-differential = { version = "0.0.0", path = "../boundary-differential" }
mcloving-jenkins-differential = { version = "0.0.0", path = "../jenkins-differential" }
mcloving-state-policy-differential = { version = "0.0.0", path = "../state-policy-differential" }
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.145"
sha2 = "=0.10.9"

[target.'cfg(windows)'.dependencies]
mcloving-windows-job = { version = "0.0.0", path = "../windows-job" }

[dev-dependencies]
tempfile = "=3.27.0"

[lints]
workspace = true
